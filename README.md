# yyauth

### docker run
```
docker run -d \
  --name yyauth \
  --restart always \
  --network host \
  ghcr.io/linxiqt/yyauth
```

国内镜像
```
docker run -d \
  --name yyauth \
  --restart always \
  --network host \
  ghcr.nju.edu.cn/linxiqt/yyauth
```
